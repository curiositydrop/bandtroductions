import { initializeApp, getApps } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js';
import { getDatabase, ref, onValue, set } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-database.js';
import { activePlaylist, playlistPosition, formatMinutes, RADIO_TIMEZONE } from './radio-schedule-engine.js?v=2';

const cfg={apiKey:'AIzaSyApLiiJsKTw1Fp8J3aQatMqiSZoP_6EycE',authDomain:'bandfanwall.firebaseapp.com',databaseURL:'https://bandfanwall-default-rtdb.firebaseio.com',projectId:'bandfanwall',storageBucket:'bandfanwall.firebasestorage.app',messagingSenderId:'619241154826',appId:'1:619241154826:web:25ddc58eef094e3c0732f3'};
const app=getApps().find(a=>a.name==='radioPlaylistSaveFixV5')||initializeApp(cfg,'radioPlaylistSaveFixV5');
const db=getDatabase(app);
let stationPlaylists={},tracks={},sponsors={},shadowDraft=[];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const mins=v=>{const [h,m]=String(v||'00:00').split(':').map(Number);return (h||0)*60+(m||0)};
const durationText=s=>{s=Math.max(0,Math.round(Number(s)||0));return `${Math.floor(s/60)}:${String(s%60).padStart(2,'0')}`};
const norm=v=>String(v||'').replace(/[🎵📢♪♫]/gu,'').trim().toLowerCase();
const parseDuration=v=>{const m=String(v||'').match(/(\d+):(\d{2})/);return m?Number(m[1])*60+Number(m[2]):0;};
const selectedDays=()=>{const v=[...document.querySelectorAll('.crr-days input:checked')].map(i=>i.value);return v.includes('every')?['every']:(v.length?v:['every']);};
const newId=()=>`playlist-${Date.now()}-${Math.random().toString(36).slice(2,7)}`;
function toDraftItem(type,id){const x=type==='track'?tracks[id]:sponsors[id];if(!x)return null;return {type,id,title:type==='track'?(x.title||'Untitled'):(x.businessName||'Sponsor'),artist:type==='track'?(x.artist||'Unknown Artist'):'Sponsor',audioUrl:x.audioUrl||'',coverUrl:type==='track'?(x.coverUrl||''):(x.logoUrl||''),profileUrl:x.profileUrl||'',durationSeconds:Number(x.durationSeconds||x.audioDurationSeconds)||0};}
function syncShadowFromVisible(){const visible=[...document.querySelectorAll('#crr-drop [data-draft-index]')];if(!visible.length){shadowDraft=[];return;}const rebuilt=[];for(const card of visible){const strong=card.querySelector('strong')?.textContent||'',title=norm(strong),isSponsor=strong.includes('📢'),meta=[...card.querySelectorAll('span')].filter(s=>!s.classList.contains('crr-handle')).map(s=>s.textContent||'').join(' '),dur=parseDuration(meta),src=isSponsor?sponsors:tracks;let hit=Object.entries(src).find(([id,x])=>id!=='__station'&&!x?.isStationPlaylist&&norm(isSponsor?x.businessName:x.title)===title);if(hit){const item=toDraftItem(isSponsor?'sponsor':'track',hit[0]);if(item){if(!item.durationSeconds)item.durationSeconds=dur;rebuilt.push(item);continue;}}const old=shadowDraft[Number(card.dataset.draftIndex)];if(old)rebuilt.push({...old,durationSeconds:Number(old.durationSeconds)||dur});}if(rebuilt.length)shadowDraft=rebuilt;}
function extractSchedules(all){const out={};for(const [key,x] of Object.entries(all||{})){if(!x?.isStationPlaylist||!x?.playlistJson)continue;try{const p=JSON.parse(x.playlistJson);if(p?.items?.length)out[p.id||key.replace(/^__playlist_/,'')]=p;}catch{}}return out;}
async function savePlaylistFromUi(){const status=document.getElementById('crr-builder-status'),button=document.getElementById('crr-save'),name=document.getElementById('crr-name')?.value.trim()||'',start=mins(document.getElementById('crr-start')?.value),end=mins(document.getElementById('crr-end')?.value);syncShadowFromVisible();const items=shadowDraft.filter(x=>x&&x.audioUrl).map(x=>({...x,durationSeconds:Number(x.durationSeconds)||180}));if(!name){status.textContent='Give the playlist a name.';status.style.color='#ff9d9d';return;}if(!items.length){status.textContent='The song is visible, but its approved audio data could not be read.';status.style.color='#ff9d9d';return;}if(start===end){status.textContent='Start and end time cannot be the same.';status.style.color='#ff9d9d';return;}const id=button?.dataset.stationEditId||newId(),old=stationPlaylists[id]||{},payload={id,name,days:selectedDays(),startMinutes:start,endMinutes:end,timezone:RADIO_TIMEZONE,items,totalDurationSeconds:Math.round(items.reduce((n,x)=>n+Number(x.durationSeconds||0),0)*10)/10,active:true,createdAt:old.createdAt||Date.now(),updatedAt:Date.now()};const record={artist:'BANDtroductions Radio Schedule',title:name,audioUrl:items[0].audioUrl,coverUrl:items[0].coverUrl||'',durationSeconds:payload.totalDurationSeconds,approved:false,reviewStatus:'system-playlist',isStationPlaylist:true,playlistJson:JSON.stringify(payload),dateAdded:Date.now(),updatedAt:Date.now()};button.disabled=true;status.style.color='#9ca5a4';status.textContent='Saving playlist and schedule…';try{await set(ref(db,`RadioTracks/__playlist_${id}`),record);stationPlaylists[id]=payload;status.style.color='#72e7d9';status.textContent=`✓ ${name} created and scheduled.`;button.dataset.stationEditId='';renderSchedules();}catch(error){console.error('Station playlist save failed',error);status.style.color='#ff9d9d';status.textContent=`Playlist save failed: ${error?.code||error?.message||'PERMISSION_DENIED'}`;}finally{button.disabled=false;}}
function renderSchedules(){const el=document.getElementById('crr-schedules');if(!el)return;const current=activePlaylist(stationPlaylists),entries=Object.entries(stationPlaylists).sort((a,b)=>(a[1].startMinutes||0)-(b[1].startMinutes||0)),count=document.getElementById('crr-playlist-count');if(count)count.textContent=entries.length;el.innerHTML=entries.length?entries.map(([id,p])=>{const on=current?.id===id;return `<article class="crr-schedule ${on?'onair':''}"><strong style="color:#fff">${on?'🔴 ':''}${esc(p.name||'Playlist')}</strong><div class="crr-muted">${(p.days||['every']).join(', ')} · ${formatMinutes(p.startMinutes)}–${formatMinutes(p.endMinutes)} · ${durationText(p.totalDurationSeconds)} · ${(p.items||[]).length} items · ${p.active===false?'INACTIVE':'ACTIVE'}</div></article>`}).join(''):'<div class="crr-empty">No scheduled playlists yet.</div>';renderOnAir();}
function renderOnAir(){const el=document.getElementById('crr-onair');if(!el)return;const p=activePlaylist(stationPlaylists);if(!p){el.innerHTML='<div class="crr-empty">Nothing is scheduled for this time slot.</div>';return;}const pos=playlistPosition(p),item=p.items?.[pos?.index||0];if(!item){el.innerHTML='<div class="crr-empty">Active playlist has no playable items.</div>';return;}el.innerHTML=`<div class="crr-onair"><div class="crr-onair-art">${item.coverUrl?`<img src="${esc(item.coverUrl)}">`:(item.type==='sponsor'?'📢':'♪')}</div><div><strong style="color:#fff;font-size:1rem">${esc(p.name)}</strong><div style="color:#0ccfbd;font-weight:900;margin-top:4px">${item.type==='sponsor'?'SPONSOR: ':''}${esc(item.title||'Untitled')}</div><div class="crr-muted">${esc(item.artist||'')} · ${durationText(pos?.offsetSeconds||0)} into item · ${formatMinutes(p.startMinutes)}–${formatMinutes(p.endMinutes)}</div></div></div>`;}
document.addEventListener('click',async e=>{const add=e.target.closest('[data-action="add-library"]');if(add){const item=toDraftItem(add.dataset.type,add.dataset.key);if(item)shadowDraft.push(item);return;}const remove=e.target.closest('[data-action="remove-draft"]');if(remove){shadowDraft.splice(Number(remove.dataset.index),1);return;}if(e.target.closest('#crr-clear')||e.target.closest('#crr-new')){shadowDraft=[];return;}const save=e.target.closest('#crr-save');if(save){e.preventDefault();e.stopImmediatePropagation();await savePlaylistFromUi();}},true);
onValue(ref(db,'RadioTracks'),s=>{tracks=s.val()||{};stationPlaylists=extractSchedules(tracks);renderSchedules();},e=>console.error('RadioTracks read failed',e));
onValue(ref(db,'RadioSponsors'),s=>{sponsors=s.val()||{};},()=>{});
setInterval(renderOnAir,1000);
