import { initializeApp, getApps } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js';
import { getDatabase, ref, onValue, set } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-database.js';
import { activePlaylist, playlistPosition, formatMinutes, RADIO_TIMEZONE } from './radio-schedule-engine.js?v=2';

const cfg={apiKey:'AIzaSyApLiiJsKTw1Fp8J3aQatMqiSZoP_6EycE',authDomain:'bandfanwall.firebaseapp.com',databaseURL:'https://bandfanwall-default-rtdb.firebaseio.com',projectId:'bandfanwall',storageBucket:'bandfanwall.firebasestorage.app',messagingSenderId:'619241154826',appId:'1:619241154826:web:25ddc58eef094e3c0732f3'};
const app=getApps().find(a=>a.name==='radioPlaylistSaveFixV3')||initializeApp(cfg,'radioPlaylistSaveFixV3');
const db=getDatabase(app);
const STATION_PATH='RadioTracks/__station';
let stationPlaylists={};
let tracks={};
let sponsors={};
let shadowDraft=[];

const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[c]));
const mins=v=>{const [h,m]=String(v||'00:00').split(':').map(Number);return (h||0)*60+(m||0)};
const durationText=s=>{s=Math.max(0,Math.round(Number(s)||0));return `${Math.floor(s/60)}:${String(s%60).padStart(2,'0')}`};
const timeValue=m=>`${String(Math.floor((Number(m)||0)/60)%24).padStart(2,'0')}:${String((Number(m)||0)%60).padStart(2,'0')}`;
function selectedDays(){const vals=[...document.querySelectorAll('.crr-days input:checked')].map(i=>i.value);return vals.includes('every')?['every']:(vals.length?vals:['every']);}
function newId(){return `playlist-${Date.now()}-${Math.random().toString(36).slice(2,7)}`;}
function toDraftItem(type,id){const x=type==='track'?tracks[id]:sponsors[id];if(!x)return null;return {type,id,title:type==='track'?(x.title||'Untitled'):(x.businessName||'Sponsor'),artist:type==='track'?(x.artist||'Unknown Artist'):'Sponsor',audioUrl:x.audioUrl||'',coverUrl:type==='track'?(x.coverUrl||''):(x.logoUrl||''),profileUrl:x.profileUrl||'',durationSeconds:Number(x.durationSeconds||x.audioDurationSeconds)||0};}
function syncShadowFromVisible(){const visible=[...document.querySelectorAll('#crr-drop [data-draft-index]')];if(!visible.length){shadowDraft=[];return;}if(shadowDraft.length===visible.length)return;const rebuilt=[];for(const card of visible){const text=(card.querySelector('strong')?.textContent||'').toLowerCase();const sponsor=text.includes('📢');const title=text.replace(/[🎵📢♪♫]/gu,'').trim();const source=sponsor?sponsors:tracks;const hit=Object.entries(source).find(([id,x])=>id!=='__station'&&String(sponsor?x.businessName:x.title||'').toLowerCase()===title);if(hit){const item=toDraftItem(sponsor?'sponsor':'track',hit[0]);if(item)rebuilt.push(item);}}if(rebuilt.length)shadowDraft=rebuilt;}

async function savePlaylistFromUi(){const status=document.getElementById('crr-builder-status'),button=document.getElementById('crr-save');const name=document.getElementById('crr-name')?.value.trim()||'';const start=mins(document.getElementById('crr-start')?.value),end=mins(document.getElementById('crr-end')?.value);syncShadowFromVisible();const items=shadowDraft.filter(x=>x&&x.audioUrl&&Number(x.durationSeconds)>0);if(!name){status.textContent='Give the playlist a name.';status.style.color='#ff9d9d';return;}if(!items.length){status.textContent='The song is on screen, but the save layer did not receive its library ID. Refresh Admin once, tap + Add, then Create Playlist.';status.style.color='#ff9d9d';return;}if(start===end){status.textContent='Start and end time cannot be the same.';status.style.color='#ff9d9d';return;}const editingId=button?.dataset.stationEditId||'';const id=editingId||newId();const old=stationPlaylists[id]||{};const payload={name,days:selectedDays(),startMinutes:start,endMinutes:end,timezone:RADIO_TIMEZONE,items,totalDurationSeconds:Math.round(items.reduce((n,x)=>n+Number(x.durationSeconds||0),0)*10)/10,active:true,createdAt:old.createdAt||Date.now(),updatedAt:Date.now()};button.disabled=true;status.style.color='#9ca5a4';status.textContent='Saving playlist and schedule…';try{const next={...stationPlaylists,[id]:payload};await set(ref(db,STATION_PATH),next);stationPlaylists=next;status.style.color='#72e7d9';status.textContent=`✓ ${name} created and scheduled.`;button.dataset.stationEditId='';renderSchedules();}catch(error){console.error('Station playlist save failed',error);status.style.color='#ff9d9d';status.textContent=`Playlist save failed: ${error?.code||error?.message||'PERMISSION_DENIED'}`;}finally{button.disabled=false;}}

function renderSchedules(){const el=document.getElementById('crr-schedules');if(!el)return;const current=activePlaylist(stationPlaylists);const entries=Object.entries(stationPlaylists).sort((a,b)=>(a[1].startMinutes||0)-(b[1].startMinutes||0));const count=document.getElementById('crr-playlist-count');if(count)count.textContent=entries.length;el.innerHTML=entries.length?entries.map(([id,p])=>{const on=current?.id===id;return `<article class="crr-schedule ${on?'onair':''}"><strong style="color:#fff">${on?'🔴 ':''}${esc(p.name||'Playlist')}</strong><div class="crr-muted">${(p.days||['every']).join(', ')} · ${formatMinutes(p.startMinutes)}–${formatMinutes(p.endMinutes)} · ${durationText(p.totalDurationSeconds)} · ${(p.items||[]).length} items · ${p.active===false?'INACTIVE':'ACTIVE'}</div></article>`}).join(''):'<div class="crr-empty">No scheduled playlists yet.</div>';renderOnAir();}
function renderOnAir(){const el=document.getElementById('crr-onair');if(!el)return;const p=activePlaylist(stationPlaylists);if(!p){el.innerHTML='<div class="crr-empty">Nothing is scheduled for this time slot.</div>';return;}const pos=playlistPosition(p);const item=p.items?.[pos?.index||0];if(!item){el.innerHTML='<div class="crr-empty">Active playlist has no playable items.</div>';return;}el.innerHTML=`<div class="crr-onair"><div class="crr-onair-art">${item.coverUrl?`<img src="${esc(item.coverUrl)}">`:(item.type==='sponsor'?'📢':'♪')}</div><div><strong style="color:#fff;font-size:1rem">${esc(p.name)}</strong><div style="color:#0ccfbd;font-weight:900;margin-top:4px">${item.type==='sponsor'?'SPONSOR: ':''}${esc(item.title||'Untitled')}</div><div class="crr-muted">${esc(item.artist||'')} · ${durationText(pos?.offsetSeconds||0)} into item · ${formatMinutes(p.startMinutes)}–${formatMinutes(p.endMinutes)}</div></div></div>`;}

document.addEventListener('click',async e=>{const add=e.target.closest('[data-action="add-library"]');if(add){const item=toDraftItem(add.dataset.type,add.dataset.key);if(item)shadowDraft.push(item);return;}const remove=e.target.closest('[data-action="remove-draft"]');if(remove){shadowDraft.splice(Number(remove.dataset.index),1);return;}if(e.target.closest('#crr-clear')||e.target.closest('#crr-new')){shadowDraft=[];return;}const save=e.target.closest('#crr-save');if(save){e.preventDefault();e.stopImmediatePropagation();await savePlaylistFromUi();}},true);

onValue(ref(db,'RadioTracks'),s=>{tracks=s.val()||{};stationPlaylists=tracks.__station||{};renderSchedules();},e=>console.error('RadioTracks read failed',e));
onValue(ref(db,'RadioSponsors'),s=>{sponsors=s.val()||{};},()=>{});
setInterval(renderOnAir,1000);
